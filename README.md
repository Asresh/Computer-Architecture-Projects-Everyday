# Computer Architecture Projects — Everyday

A daily series of self-contained computer-architecture projects in SystemVerilog. One documented, simulate-able architecture design per day.

Each day lives in its own folder (`DayN/`) containing a self-contained project with source, a self-checking testbench, a Makefile, a waveform image, and a README write-up.

## Index

| Day | Project | Key concepts | Folder |
|-----|---------|--------------|--------|
| 1 | Single-cycle RISC-V (RV32I) Integer ALU | Combinational ALU, RV32I ops, signed vs. unsigned compare, logical vs. arithmetic shift, zero flag | [Day1](Day1/) |
| 2 | RV32I Register File (2R / 1W) | Architectural register state, dual combinational read ports, synchronous write, x0 hardwired to zero, internal write-forwarding (WB→ID hazard) | [Day2](Day2/) |
| 3 | RV32I Instruction Decoder + Immediate Generator | Combinational control decode (opcode/funct3/funct7), datapath control lines, ALU-control mapping, I/S/B/U/J immediate generation, safe NOP default | [Day3](Day3/) |
| 4 | RV32I Load/Store Unit + Data Memory | Byte-addressable little-endian memory, lb/lh/lw/lbu/lhu/sb/sh/sw, byte-lane selection, sign vs. zero extension, per-byte write enables, within-word misalignment policy | [Day4](Day4/) |
| 5 | Single-Cycle RV32I Core (integer subset) | Full integration (ALU + regfile + decoder + LSU + PC/imem), operand & write-back muxes, ALU-reuse branch resolution, jal/jalr next-PC, directed program checked vs. an independent ISS golden trace | [Day5](Day5/) |
| 6 | Bimodal 2-bit Saturating-Counter Branch Predictor | Dynamic branch prediction, PC-indexed Pattern History Table, 2-bit saturating-counter FSM, hysteresis / single-mispredict loop-exit tolerance, independent predict (fetch) & update (resolve) ports, per-PC independence | [Day6](Day6/) |
| 7 | Direct-Mapped Write-Back / Write-Allocate Data Cache | Memory hierarchy, direct-mapped placement (tag/index/offset), valid & dirty bits, write-back + write-allocate policy, conflict-miss eviction with dirty write-back, miss-handling FSM (LOOKUP→WRITEBACK→ALLOCATE), word-burst memory port, CPU ready handshake | [Day7](Day7/) |
| 8 | Radix-4 (Modified) Booth Signed Sequential Multiplier | Integer multiply datapath, modified Booth recoding (2 bits/cycle), signed two's-complement multiply with no sign-correction, add/shift accumulator with arithmetic right shift, ±M/±2M partial-multiple selection, WIDTH/2-iteration latency, start/busy/done handshake FSM | [Day8](Day8/) |
| 9 | Round-Robin (Rotating-Priority) Arbiter | On-chip resource arbitration, rotating-priority fairness, provable no-starvation bound (grant within N cycles), one-hot grant + binary index, mask/isolate-lowest-set-bit priority pick (no variable bit-selects), single registered rotate pointer | [Day9](Day9/) |
| 10 | Fully-Associative TLB with True-LRU Replacement | Virtual memory & address translation, VPN→PPN mapping with page-offset passthrough, fully-associative CAM lookup (parallel tag compare), hit/miss & single-cycle response, true-LRU replacement via an N×N reference (age) matrix, refill priority (overwrite→free-slot→LRU victim), single-page invalidate & full flush (sfence) | [Day10](Day10/) |
| 11 | Store Buffer with Store-to-Load Forwarding | Store retirement decoupling, in-order circular-FIFO store queue, store-to-load forwarding of the youngest matching store, load-miss fall-through, program-order drain to memory, simultaneous push+drain, full/empty backpressure handshakes, relative-age youngest-match select (no variable bit-selects) | [Day11](Day11/) |
| 12 | Reorder Buffer (out-of-order complete, in-order retire) | Out-of-order execution, precise architectural state, in-order allocate (tail) / out-of-order complete (any entry) / in-order retire (head-only, gated on done), head-of-line stall, entry tag as result handle (renaming anchor), circular head/tail pointers with wrap bit, single-cycle flush/squash for mispredict recovery, full/empty backpressure | [Day12](Day12/) |
| 13 | Output-Stationary Systolic Array (Matrix-Multiply MAC Grid) | GPU Tensor-Core / TPU MXU dataflow, N×N grid of MAC PEs, output-stationary per-PE accumulation, west→east A / north→south B operand streaming with diagonal skew, spatial O(N³) MACs at O(N) edge bandwidth, nearest-neighbour-only wiring, deterministic 3N-cycle fill/drain latency, start/busy/done handshake, signed two's-complement matmul | [Day13](Day13/) |
| 14 | SIMT Reconvergence (PDOM) Stack | GPU SIMT warp control flow, per-warp post-dominator stack of {mask, pc, rpc} groups, divergent-branch split (taken/not-taken paths + join entry) vs. automatic uniform-branch retarget, immediate-post-dominator reconvergence via `at_reconv` (pc==rpc) pop, arbitrary nesting, active-lane mask shrink/restore, overflow/underflow sticky error, checked against an independent golden-model stack | [Day14](Day14/) |
| 15 | GPU Memory Coalescing Unit | GPU global-memory throughput, per-warp per-lane address coalescing into aligned cache-line segments, one transaction per unique segment, deterministic lowest-lane leader election, active/transaction popcounts (coalescing efficiency), per-transaction base + served-lane mask, O(N²) all-pairs address compare, full-coalesce / scatter / broadcast / masked / empty corner cases, single-cycle registered handshake, checked against an independent golden model | [Day15](Day15/) |
| 16 | GPU Shared-Memory Bank-Conflict Detector & Access Serializer | GPU shared-memory (CUDA `__shared__`) banking, per-lane bank map `(addr/BYTES) mod NBANKS`, bank-conflict vs. broadcast detection, serialization factor `n_phases = max distinct words per bank`, word-leader election (broadcast followers are free), per-lane replay-phase schedule, unique-word/broadcast/active popcounts, N-way-conflict / broadcast / 2-way / mixed / empty corner cases, O(N²) all-pairs compare with no variable bit-selects, single-cycle registered handshake, checked against an independent per-bank golden model | [Day16](Day16/) |
| 17 | Pipelined Bitonic Sorting Network | Oblivious data-parallel sort (GPU sort / SIMT-friendly, HFT low-latency rank & top-N), Batcher bitonic network of `L·(L+1)/2` compare-exchange layers, one registered layer per step → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LAYERS+1`, partner `i XOR J` / ascending `(i & K)==0` schedule, per-launch ascending/descending via output lane reversal, purely structural (no variable bit-selects / no data-dependent branching), FIFO-scoreboard self-check vs. an independent golden sort | [Day17](Day17/) |
| 18 | Kogge-Stone Parallel Prefix-Sum (Inclusive / Exclusive Scan) | Fundamental GPU data-parallel primitive (CUB `DeviceScan` / warp `__shfl` scan; stream compaction, radix-sort offsets, HFT cumulative order-book depth & volume), Kogge-Stone `log₂N`-depth parallel-prefix recurrence (`a[i] += a[i-2^d]`), one registered stage per level → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LOG2+1` (inclusive) / `LOG2+2` (exclusive), selectable inclusive vs. exclusive, unsigned wrap-around accumulate, purely structural constant offsets (no variable bit-selects / no data-dependent branching), latency-matched delay-line self-check vs. an independent golden scan | [Day18](Day18/) |
| 19 | Pipelined Argmax / Argmin Reduction Tree (Top-of-Book Selector) | GPU warp arg-reduction primitive (CUDA `__reduce_max_sync` / CUB `DeviceReduce::ArgMax`+`ArgMin`; HFT best-bid / best-ask top-of-book extraction), balanced `log₂N`-deep binary reduction tree carrying `{value, index}` (not just the value), per-launch `mode` for argmax vs. argmin, deterministic lowest-index (first-extreme) tie-break realised in a single value compare via the left-child-lower-index invariant, one registered compare-select row per level → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LOG2+1`, pipelined mode+valid so back-to-back launches freely mix argmax/argmin, purely structural constant-index wiring (no variable bit-selects / no data-dependent branching), latency-matched delay-line self-check vs. an independent first-extreme golden model | [Day19](Day19/) |
| 20 | Systolic Shift-Register Priority Queue (hardware min-heap) | Ultra-low-latency HFT order-book / matching-engine best-price queue & GPU streaming Top-K substrate (`torch.topk` / CUB `DeviceSelect`; hardware task/warp scheduler), single-cycle `O(1)` ENQUEUE (parallel sorted insert) and EXTRACT-MIN (free peek + one-cycle left shift), `DEPTH` register cells kept packed & sorted so cell[0] is always the minimum, monotone `cond` insertion-point pick (keep / take-new / shift-in-left) resolving all cells in one combinational step, price-time (FIFO-at-price) ordering of equal keys via `enq_key < key[i]` stable insert, one op/cycle at fixed data-independent latency, sticky overflow (ENQ-while-full drops) / underflow (DEQ-while-empty ignored), no variable bit-selects, lock-step self-check vs. an independent sorted-array golden model (directed + full/overflow + 4000 random ops) | [Day20](Day20/) |
| 21 | Fully-Pipelined Dual-Mode CORDIC Engine (cos/sin + magnitude/atan2) | GPU Special Function Unit (SFU) transcendental core (`MUFU.SIN`/`__sinf`, rectangular→polar) & ultra-low-latency HFT / FPGA shift-add datapath, circular CORDIC in ROTATION mode (seed `x0=K≈0.6073` → `x_out=cos θ`, `y_out=sin θ`) and VECTORING mode (`→ K·√(x0²+y0²)`, `atan2(y0,x0)`), **multiplier-free / zero-DSP** shifts + adds + 14-entry `atan` ROM, one rotation micro-step per registered stage → fully pipelined 1-result/cycle at **fixed data-independent latency `ITERS+1`**, gain-cancelling `K` pre-scale, per-stage constant shifts (no variable bit-selects; only add-vs-sub is data-dependent), `mode`/`valid` pipelined so back-to-back launches mix modes, `GUARD`-bit overflow margin, convergence domain `|θ|≤1.7433 rad` / vectoring `x0>0`, dual self-check (bit-exact golden CORDIC + real `$cos`/`$sin` accuracy ≤0.01) over directed corners + 3000 random ops | [Day21](Day21/) |
