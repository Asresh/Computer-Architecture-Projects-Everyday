# Computer Architecture Projects — Everyday

A daily series of self-contained computer-architecture projects in SystemVerilog. One documented, simulate-able architecture design per day.

Each day lives in its own folder (`DayN/`) containing a self-contained project with source, a self-checking testbench, a Makefile, a waveform image, and a README write-up.

## Index

| Day | Project | Key concepts | Folder |
|-----|---------|--------------|--------|
| 1 | Single-cycle RISC-V (RV32I) Integer ALU | Combinational ALU, RV32I ops, signed vs. unsigned compare, logical vs. arithmetic shift, zero flag | [Day1](Day1/) |
| 2 | RV32I Register File (2R / 1W) | Architectural register state, dual combinational read ports, synchronous write, x0 hardwired to zero, internal write-forwarding (WB→ID hazard) | [Day2](Day2/) |
| 3 | RV32I Instruction Decoder + Immediate Generator | Combinational control decode (opcode/funct3/funct7), datapath control lines, ALU-control mapping, I/S/B/U/J immediate generation, safe NOP default | [Day3](Day3/) |
| 4 | RV32I Load/Store Unit + Data Memory | Byte-addressable little-endian memory, lb/lh/lw/lbu/lhu/sb/sh/sw, byte-lane selection, sign vs. zero extension, per-byte write enables, within-word misalignment policy | [Day4](Day4/) |
| 5 | Single-Cycle RV32I Core (integer subset) | Full integration (ALU + regfile + decoder + LSU + PC/imem), operand & write-back muxes, ALU-reuse branch resolution, jal/jalr next-PC, directed program checked vs. an independent ISS golden trace | [Day5](Day5/) |
| 6 | Bimodal 2-bit Saturating-Counter Branch Predictor | Dynamic branch prediction, PC-indexed Pattern History Table, 2-bit saturating-counter FSM, hysteresis / single-mispredict loop-exit tolerance, independent predict (fetch) & update (resolve) ports, per-PC independence | [Day6](Day6/) |
| 7 | Direct-Mapped Write-Back / Write-Allocate Data Cache | Memory hierarchy, direct-mapped placement (tag/index/offset), valid & dirty bits, write-back + write-allocate policy, conflict-miss eviction with dirty write-back, miss-handling FSM (LOOKUP→WRITEBACK→ALLOCATE), word-burst memory port, CPU ready handshake | [Day7](Day7/) |
| 8 | Radix-4 (Modified) Booth Signed Sequential Multiplier | Integer multiply datapath, modified Booth recoding (2 bits/cycle), signed two's-complement multiply with no sign-correction, add/shift accumulator with arithmetic right shift, ±M/±2M partial-multiple selection, WIDTH/2-iteration latency, start/busy/done handshake FSM | [Day8](Day8/) |
| 9 | Round-Robin (Rotating-Priority) Arbiter | On-chip resource arbitration, rotating-priority fairness, provable no-starvation bound (grant within N cycles), one-hot grant + binary index, mask/isolate-lowest-set-bit priority pick (no variable bit-selects), single registered rotate pointer | [Day9](Day9/) |
| 10 | Fully-Associative TLB with True-LRU Replacement | Virtual memory & address translation, VPN→PPN mapping with page-offset passthrough, fully-associative CAM lookup (parallel tag compare), hit/miss & single-cycle response, true-LRU replacement via an N×N reference (age) matrix, refill priority (overwrite→free-slot→LRU victim), single-page invalidate & full flush (sfence) | [Day10](Day10/) |
| 11 | Store Buffer with Store-to-Load Forwarding | Store retirement decoupling, in-order circular-FIFO store queue, store-to-load forwarding of the youngest matching store, load-miss fall-through, program-order drain to memory, simultaneous push+drain, full/empty backpressure handshakes, relative-age youngest-match select (no variable bit-selects) | [Day11](Day11/) |
| 12 | Reorder Buffer (out-of-order complete, in-order retire) | Out-of-order execution, precise architectural state, in-order allocate (tail) / out-of-order complete (any entry) / in-order retire (head-only, gated on done), head-of-line stall, entry tag as result handle (renaming anchor), circular head/tail pointers with wrap bit, single-cycle flush/squash for mispredict recovery, full/empty backpressure | [Day12](Day12/) |
| 13 | Output-Stationary Systolic Array (Matrix-Multiply MAC Grid) | GPU Tensor-Core / TPU MXU dataflow, N×N grid of MAC PEs, output-stationary per-PE accumulation, west→east A / north→south B operand streaming with diagonal skew, spatial O(N³) MACs at O(N) edge bandwidth, nearest-neighbour-only wiring, deterministic 3N-cycle fill/drain latency, start/busy/done handshake, signed two's-complement matmul | [Day13](Day13/) |
| 14 | SIMT Reconvergence (PDOM) Stack | GPU SIMT warp control flow, per-warp post-dominator stack of {mask, pc, rpc} groups, divergent-branch split (taken/not-taken paths + join entry) vs. automatic uniform-branch retarget, immediate-post-dominator reconvergence via `at_reconv` (pc==rpc) pop, arbitrary nesting, active-lane mask shrink/restore, overflow/underflow sticky error, checked against an independent golden-model stack | [Day14](Day14/) |
| 15 | GPU Memory Coalescing Unit | GPU global-memory throughput, per-warp per-lane address coalescing into aligned cache-line segments, one transaction per unique segment, deterministic lowest-lane leader election, active/transaction popcounts (coalescing efficiency), per-transaction base + served-lane mask, O(N²) all-pairs address compare, full-coalesce / scatter / broadcast / masked / empty corner cases, single-cycle registered handshake, checked against an independent golden model | [Day15](Day15/) |
| 16 | GPU Shared-Memory Bank-Conflict Detector & Access Serializer | GPU shared-memory (CUDA `__shared__`) banking, per-lane bank map `(addr/BYTES) mod NBANKS`, bank-conflict vs. broadcast detection, serialization factor `n_phases = max distinct words per bank`, word-leader election (broadcast followers are free), per-lane replay-phase schedule, unique-word/broadcast/active popcounts, N-way-conflict / broadcast / 2-way / mixed / empty corner cases, O(N²) all-pairs compare with no variable bit-selects, single-cycle registered handshake, checked against an independent per-bank golden model | [Day16](Day16/) |
| 17 | Pipelined Bitonic Sorting Network | Oblivious data-parallel sort (GPU sort / SIMT-friendly, HFT low-latency rank & top-N), Batcher bitonic network of `L·(L+1)/2` compare-exchange layers, one registered layer per step → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LAYERS+1`, partner `i XOR J` / ascending `(i & K)==0` schedule, per-launch ascending/descending via output lane reversal, purely structural (no variable bit-selects / no data-dependent branching), FIFO-scoreboard self-check vs. an independent golden sort | [Day17](Day17/) |
| 18 | Kogge-Stone Parallel Prefix-Sum (Inclusive / Exclusive Scan) | Fundamental GPU data-parallel primitive (CUB `DeviceScan` / warp `__shfl` scan; stream compaction, radix-sort offsets, HFT cumulative order-book depth & volume), Kogge-Stone `log₂N`-depth parallel-prefix recurrence (`a[i] += a[i-2^d]`), one registered stage per level → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LOG2+1` (inclusive) / `LOG2+2` (exclusive), selectable inclusive vs. exclusive, unsigned wrap-around accumulate, purely structural constant offsets (no variable bit-selects / no data-dependent branching), latency-matched delay-line self-check vs. an independent golden scan | [Day18](Day18/) |
| 19 | Pipelined Argmax / Argmin Reduction Tree (Top-of-Book Selector) | GPU warp arg-reduction primitive (CUDA `__reduce_max_sync` / CUB `DeviceReduce::ArgMax`+`ArgMin`; HFT best-bid / best-ask top-of-book extraction), balanced `log₂N`-deep binary reduction tree carrying `{value, index}` (not just the value), per-launch `mode` for argmax vs. argmin, deterministic lowest-index (first-extreme) tie-break realised in a single value compare via the left-child-lower-index invariant, one registered compare-select row per level → fully pipelined 1-vector/cycle throughput at fixed data-independent latency `LOG2+1`, pipelined mode+valid so back-to-back launches freely mix argmax/argmin, purely structural constant-index wiring (no variable bit-selects / no data-dependent branching), latency-matched delay-line self-check vs. an independent first-extreme golden model | [Day19](Day19/) |
| 20 | Systolic Shift-Register Priority Queue (hardware min-heap) | Ultra-low-latency HFT order-book / matching-engine best-price queue & GPU streaming Top-K substrate (`torch.topk` / CUB `DeviceSelect`; hardware task/warp scheduler), single-cycle `O(1)` ENQUEUE (parallel sorted insert) and EXTRACT-MIN (free peek + one-cycle left shift), `DEPTH` register cells kept packed & sorted so cell[0] is always the minimum, monotone `cond` insertion-point pick (keep / take-new / shift-in-left) resolving all cells in one combinational step, price-time (FIFO-at-price) ordering of equal keys via `enq_key < key[i]` stable insert, one op/cycle at fixed data-independent latency, sticky overflow (ENQ-while-full drops) / underflow (DEQ-while-empty ignored), no variable bit-selects, lock-step self-check vs. an independent sorted-array golden model (directed + full/overflow + 4000 random ops) | [Day20](Day20/) |
| 21 | Fully-Pipelined Dual-Mode CORDIC Engine (cos/sin + magnitude/atan2) | GPU Special Function Unit (SFU) transcendental core (`MUFU.SIN`/`__sinf`, rectangular→polar) & ultra-low-latency HFT / FPGA shift-add datapath, circular CORDIC in ROTATION mode (seed `x0=K≈0.6073` → `x_out=cos θ`, `y_out=sin θ`) and VECTORING mode (`→ K·√(x0²+y0²)`, `atan2(y0,x0)`), **multiplier-free / zero-DSP** shifts + adds + 14-entry `atan` ROM, one rotation micro-step per registered stage → fully pipelined 1-result/cycle at **fixed data-independent latency `ITERS+1`**, gain-cancelling `K` pre-scale, per-stage constant shifts (no variable bit-selects; only add-vs-sub is data-dependent), `mode`/`valid` pipelined so back-to-back launches mix modes, `GUARD`-bit overflow margin, convergence domain `|θ|≤1.7433 rad` / vectoring `x0>0`, dual self-check (bit-exact golden CORDIC + real `$cos`/`$sin` accuracy ≤0.01) over directed corners + 3000 random ops | [Day21](Day21/) |
| 22 | Fully-Pipelined Newton-Raphson Fixed-Point Reciprocal Unit (`1/x`) | GPU Special-Function-Unit reciprocal primitive (`MUFU.RCP`/`__frcp_rn`) & ultra-low-latency HFT / FPGA divide substrate (VWAP, price ratios, book imbalance, per-share normalisation, implied-vol Newton steps — division turned into a fixed-latency stream), range-reduced Newton-Raphson: LZC barrel-shift **normalise** `x→m∈[1,2)`+exponent → 256-entry **seed ROM** `y0≈1/m` (≥8 bits) → **two** NR refines `y←y·(2−m·y)` (quadratic convergence, correct bits 8→16→32) → rounded **denormalise** to `Y=round(2^46/x)`, one step per registered stage → fully pipelined 1-result/cycle at **fixed data-independent latency `LATENCY=7`** (the HFT tick-to-trade property; no iterative-divider early-out), four `MW×MW` DSP48/DSP58 multiplies + LUT-fabric LZC/shifts, `div0` flag + all-ones saturation on `x==0`, ≥24-bit accuracy (measured worst case ~26 correct bits), self-check vs an **independent** 128-bit integer golden `round(2^46/x)` over directed corners (powers-of-two/min/max/zero) + 4000 back-to-back random ops (up to 7 in flight) | [Day22](Day22/) |
| 23 | N-Way Set-Associative Write-Back Cache with Tree-PLRU Replacement | Set-associative memory hierarchy, parallel tag compare across WAYS, invalid-way-first fill then tree-PLRU (`WAYS-1` node bits/set) victim descent + touch/promote-to-MRU, write-back (lazy dirty flush) + write-allocate (store-miss block fetch), miss FSM `LOOKUP→WRITEBACK→ALLOCATE→LOOKUP` with word-burst memory port, dirty-victim eviction, CPU ready handshake, no data-dependent variable bit-selects, lockstep self-check vs. an independent golden cache (data + hit + full valid/tag/dirty state) over a directed dirty-eviction window + 4000 random ops | [Day23](Day23/) |
| 24 | gshare Correlating Branch Predictor | Dynamic correlating prediction (McFarling gshare), Global History Register XOR-folded with the PC to index a PHT of 2-bit saturating counters, per-history counter specialization that captures inter-branch correlation & alternating patterns a bimodal predictor cannot, XOR fold (zero-extend/truncate history to index width) for dense `PC × history` coverage, independent combinational predict (fetch) & synchronous update (resolve) ports, fetch-time `ghist_update` history-checkpoint snapshot so the trained counter matches the predicted one, speculation-free GHR shift at retire, hysteresis via the 2-bit saturating FSM, reset-safe/lint-clean RTL, lockstep self-check vs. an independent golden model (prediction + both folded indices + pre-update counter + GHR) over a directed history-fill/correlation window + 4000 random ops (20 148 assertions) | [Day24](Day24/) |
| 25 | Classic 5-Stage Pipelined RV32I Core (forwarding + hazard stall + branch flush) | Instruction pipelining (Patterson & Hennessy IF/ID/EX/MEM/WB), one-instruction-per-cycle overlap via pipeline registers, full **data-forwarding / bypass network** (EX/MEM and MEM/WB → EX operand muxes, younger-producer priority) plus a **write-first register file** that resolves the distance-3 RAW hazard, **load-use hazard detection** inserting a one-cycle stall/bubble (the one case forwarding can't cover — load data isn't ready until end of MEM), forwarded store data & forwarding *into the branch comparator*, **EX-stage branch/jump resolution** with a 2-instruction control-hazard flush (`pc+imm` targets, `jalr = (rs1+imm)&~1`, `pc+4` link), full RV32I integer + LB/LH/LW/LBU/LHU/SB/SH/SW subset, `x0` hardwired, reset-safe/lint-clean RTL, self-check of final architectural state vs. an independent in-testbench sequential ISS over a directed all-hazard-class program + 40 randomised dense-RAW straight-line programs (22 304 assertions) | [Day25](Day25/) |
| 26 | Branch Target Buffer (BTB) + Return Address Stack (RAS) | Fetch-stage **target** prediction (the "where does it jump?" the Day 6 / Day 24 direction predictors don't answer), direct-mapped PC-indexed BTB of `{valid, tag, target, type}` learned at resolve so fetch can redirect the next cycle before decode, **taken-cache** semantics (entry presence = predict-taken) with **not-taken-conditional eviction** self-correction, four branch types (cond/jump/call/ret), a **Return Address Stack** predicting returns from call/return LIFO nesting (call pushes `pc+4`, ret pops) with saturating overflow (deepest frames preserved) + sticky underflow, independent combinational predict (fetch) & synchronous update (resolve) ports, architectural-RAS simplification (documented), no data-dependent bit-selects, reset-safe/lint-clean RTL, lockstep self-check vs. an independent behavioural golden model (predict hit/taken/type/target/ras_used + full RAS state) over a directed allocate/hit/push/pop/evict/underflow window + a RAS-overflow burst + 4000 randomised aliasing-pool ops (8060 checks) | [Day26](Day26/) |
| 27 | Stride Prefetcher (Reference Prediction Table) | Hardware data prefetching / memory-latency hiding (Chen–Baer RPT), PC-indexed direct-mapped table correlating the address pattern with the *load instruction* (separates interleaved streams), per-entry `{last_addr, stride, 2-bit confidence}`, classic 4-state FSM `INIT→TRANSIENT→STEADY` (+`NO_PRED`) demanding two stride confirmations before predicting with single-miss hysteresis (`STEADY→INIT`, keeps the learned stride), combinational predict `pf_addr = addr + LOOKAHEAD·stride` gated on STEADY + non-zero stride, signed/modular strides for ascending *and* descending walks, synchronous "read-now / train-for-next" update, direct-mapped conflict eviction, parameterised look-ahead distance, reset-safe/lint-clean RTL with no data-dependent bit-selects, lockstep self-check vs. an independent golden RPT (prefetch + hit + full state/stride) over a directed allocate/learn/steady/independence/relock/evict window + 4000 randomised aliasing-pool ops (4013 checks) | [Day27](Day27/) |
| 28 | Explicit Register Renaming Unit (RAT + Free List) | Out-of-order front-end dependency breaking, Register Alias Table (`RAT[a]`=current physical mapping, identity reset) + circular free-list FIFO of spare physicals, per-instruction rename (`psrc=RAT[rs]`, pop `pdst`, save displaced `pold`, install `RAT[rd]<=pdst`) that eliminates false WAW/WAR hazards leaving only true RAW, explicit-PRF (R10000/21264-style) life-cycle with deferred `pold` recycle through an independent commit/free port, empty-pool `stall` backpressure, `x0`-never-renamed, explicit modulo-`NPHYS` ring wrap (any pool size), combinational rename / synchronous update, reset-safe/lint-clean RTL with no data-dependent bit-selects, lockstep self-check vs. an independent golden RAT + free-list model (psrc1/psrc2/pdst/pold/alloc/stall/free-count) over a directed allocate/WAW/RAW/x0/drain/stall/recycle/simultaneous-alloc+free window + 5000 randomised ops (35 091 assertions) | [Day28](Day28/) |
| 29 | Out-of-Order Issue Queue (Reservation Stations) | The scheduler between Day 28 rename and Day 12 retire: dispatched instructions wait only on their own operands and launch out of program order, **associative (CAM) wakeup** comparing both source tags of every entry against all `NWAKE` broadcast result tags in parallel (Tomasulo's common-data-bus snoop, with physical-register tags), **same-cycle wakeup→select bypass** giving back-to-back dependent issue (and exposing why that loop is a real core's critical path), a **dispatch-cycle wakeup bypass** so a tag broadcast while an instruction is still in flight to the queue is not missed forever, **age-matrix oldest-ready-first select** (`age[i][j]`=i-older-than-j, maintained by one-hot ORs only: `age[a]<=0` / `age[j]<=age[j]\|a`) collapsing arbitration to one AND-OR level `grant[i]=req[i]&~\|(req&age[:][i])` that is inherently one-hot and provably starvation-free, position-independent allocation (`free&-free`) since age is tracked separately, FU backpressure (granted-but-refused entries re-arbitrate), full/empty handshakes, single-cycle flush, no data-dependent bit-selects, lockstep self-check vs. an independent **sequence-number** golden scheduler (a different definition of "oldest") plus a per-cycle grant one-hot assertion, over a directed wakeup/out-of-order/dual-broadcast/backpressure/fill/flush window + 5000 randomised cycles (50 560 assertions) | [Day29](Day29/) |
| 30 | MESI Snooping Cache-Coherence Controller | Multicore cache coherence (Papamarcos & Patel *Illinois* protocol) — the shift from caches as a private latency optimisation to caches as the enforcers of the **single-writer / multiple-readers** invariant, full four-state **I/S/E/M** line permissions with all local and remote transitions, four bus commands (**BusRd** / **BusRdX** read-for-ownership / **BusUpgr** invalidate-only-no-data / **BusWB**), Illinois `bus_shared` sampling so a load fill lands in **E** when no cache answered and **S** when one did, the **silent `E→M` store** that completes in one cycle with `bus_req` flat at zero (the entire reason E exists — MSI must broadcast an invalidate on every first store to private data), **cache-to-cache flush** of dirty data on a snoop hitting M (memory is stale), write-back / write-allocate miss FSM `IDLE→WB→FILL` with dirty-victim eviction, **split-transaction bus** correctness — a replacement invalidates its victim *before* the fill so the cache never answers for a line it has given up — snoop-port array priority with one-cycle CPU deferral, and explicit **upgrade-lost race** recovery (an invalidating snoop voids an in-flight BusUpgr, which is retried as BusRdX rather than silently leaving two caches in M), reset-safe/lint-clean RTL with no data-dependent bit-selects, lockstep self-check vs. an independent **transition-table** golden model (outputs + full per-line state/tag/data) plus three protocol invariants — single-writer vs. a shadow remote cache, flush-only-if-dirty, and end-to-end **value coherence** (every load returns the architectural value; after a drain sweep main memory equals the reference word for word) — over a directed full-life-cycle window + a forced upgrade-lost race + 4000 randomised cycles (100 212 assertions) | [Day30](Day30/) |
| 31 | Non-Blocking (Lockup-Free) Cache MSHR File | The structure (Kroft, ISCA 1981) that turns the blocking caches of Day 7 / Day 23 / Day 30 into a cache a real core can use — a miss parks its bookkeeping in a **Miss Status Holding Register** instead of freezing the pipeline, buying **hit-under-miss**, **miss-under-miss** and therefore **memory-level parallelism** (N MSHRs overlap N DRAM round trips, so N misses cost ~L cycles instead of N·L — the term that dominates real memory performance and the reason Day 27 prefetch / Day 28 rename / Day 29 out-of-order issue are worth anything), the MSHR entry as `{valid, blk, done, data[WORDS], targets[NTARGET], ntgt/nrpl}` where splitting "one block fetch" from "the list of places its words must go" is what enables **secondary-miss merging** — a fully-associative CAM compare of the block address lets a second access to an already-fetching block append a *target* and ride the existing transaction with **zero** extra bus traffic (getting this wrong means two fills into one line, so it is checked as a standalone property), three independent one-action-per-cycle ports (combinational allocate/merge, **tagged out-of-order** fill return needing no second CAM, oldest-first single-port replay with word-select and `rpl_last` auto-free), the four honest reasons a lockup-free cache still blocks (**MSHR-file full** — the real MLP ceiling and a headline µarch parameter, target-list full, bus busy, entry mid-replay), `mem_req_valid` independent of `mem_req_ready` so no combinational loop runs through the bus, structural write-conflict freedom (merge needs `!done` / last-replay needs `done`; primary needs `!valid` / free needs `valid`) plus the deliberate **same-cycle late merge** where a target is appended on the exact edge its fill lands, reset-safe/lint-clean RTL with no data-dependent bit-selects, lockstep self-check vs. an independent behavioural golden model (all 16 outputs + full per-entry state, target lists and data words) reinforced by four end-to-end properties — **no duplicate fetch per block**, **every accepted access replays exactly once**, **value correctness** vs. a reference block function, and **per-block replay order** — over a directed primary/merge/MLP-3/out-of-order-fill/reuse/full-stall/bus-backpressure window, a target-list-full + late-merge + mid-replay + stray-fill corner phase, and 4000 randomised cycles with random latency, out-of-order returns and random bus/write-back back-pressure (162 482 assertions) | [Day31](Day31/) |
| 32 | FR-FCFS DRAM Memory-Access Scheduler | The level below the Day 31 MSHR file: once misses leave the chip in parallel, something has to decide the ORDER DRAM serves them in, and DRAM is a stateful device where the same access costs 1, 2 or 3 commands depending on what the previous one left in the row buffer (`COL` hit / `ACT`+`COL` / `PRE`+`ACT`+`COL` plus tRP+tRCD), **FR-FCFS** (Rixner et al., ISCA 2000) as two rules — *first-ready* (prefer an already-open row) then *first-come* (oldest wins among equals) — so a younger request may be served first purely because it is cheaper right now, **bank-level parallelism** (independent per-bank timers, so one bank activates inside another's tRCD) against the two genuinely shared resources, the one-command-per-cycle command bus and the **shared DQ data bus** (`T_BURST` gating column commands across ALL banks — the reason back-to-back row hits are 2 cycles apart), an **age-ordered collapsing queue** where a departure shifts the array down so the index IS the age rank (no sequence numbers, no age matrix — the Day 29 problem without the Day 29 machinery), a **precharge guard** (`hit_pending`) that refuses to close a row another queued request still wants, and the fairness fix textbook FR-FCFS lacks: a **bypass cap** (PAR-BS style) counting consecutive out-of-order column commands and priority-**boosting the queue head** once it saturates — waiving the precharge guard — which turns an unbounded-starvation policy into one with a provable `O(CAP·tBURST + tRP + tRCD)` head-departure bound, timing enforced by reloadable countdown timers rather than an FSM (adding tRAS/tFAW = one counter + one term), reset-safe/lint-clean RTL with no data-dependent bit-selects, checked every cycle against FOUR independent checkers — a lockstep behavioural golden model (all ten outputs + full queue/bank/timer/cap state), an independent **DRAM device model** that flags any illegal command (column access to a closed bank, ACT on an open bank, PRE on an idle bank, tRCD/tRP/tCCD/tBURST violations), a **per-tag transaction tracker** requiring exactly one matching column command issued while the DEVICE has that request's row open (the end-to-end "did it read the right row?" property), and a **starvation bound** on accept→transfer residency — over a directed activate/parallelism/hit-streak/guard/bypass/conflict-turnaround window, an adversarial cap-saturation phase, a queue-full back-pressure phase and 4000 randomised locality-biased cycles (258 004 assertions; 68% row-hit rate, 854 of 1931 requests reordered, worst-case residency 53 cycles) | [Day32](Day32/) |
